#include "third_party/catch.hpp"

#include <list>
#include <vector>
#include "TreeNode.h"

/**
 * Given a binary search tree, return a linked list of all nodes at each depth
 * (e.g. if a tree has D dpeth, return D linked lists )
 */
std::vector<std::list<TreeNode*>> getList(TreeNode* tree) {
  return std::vector<std::list<TreeNode*>>();
}