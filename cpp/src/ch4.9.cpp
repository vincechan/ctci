#include "third_party/catch.hpp"

#include "TreeNode.h"

/**
 * Given a binary tree in which each node contains a value, return all paths
 * which sum to a given value. Note that a path can start and end anywhere in
 * the tree
 */
std::vector<std::vector<TreeNode*>> findAllPathsWithSum(TreeNode* tree,
                                                        int sum) {
  // TODO: implement
  return std::vector<std::vector<TreeNode*>>();
}